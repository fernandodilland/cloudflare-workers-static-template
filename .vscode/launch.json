{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run Wrangler Dev",
            "runtimeExecutable": "wrangler",
            "args": [
                "dev"
            ],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "prepare-404-file",
            "console": "integratedTerminal"
        }
    ]
}
